/**
 * Arquivo de configuração central para a aplicação.
 * Reúne configurações importantes em um único local.
 */

export const config = {
  /**
   * O nome do modelo Gemini a ser utilizado para as tarefas de IA (transcrição e resumo).
   * 'gemini-2.5-flash' é recomendado por seu equilíbrio entre velocidade e capacidade.
   */
  modelName: 'gemini-2.5-flash',
};
